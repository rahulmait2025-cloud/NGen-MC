-- Migration 00226 intentionally reserved.
-- This file exists to keep migration numbering contiguous.
-- Do not add schema changes here because later migrations 00227+ may already be applied on shared environments.
select 1;
