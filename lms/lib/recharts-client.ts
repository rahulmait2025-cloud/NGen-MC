'use client';

/**
 * Single client entry for Recharts — ensures ResponsiveContainer sizing
 * and Recharts components stay consistent in client components.
 */
export {
  ResponsiveContainer,
  Tooltip,
  Legend,
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  AreaChart,
  Area,
  LineChart,
  Line,
  PieChart,
  Pie,
  Cell,
} from 'recharts';
