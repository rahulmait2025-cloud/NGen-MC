import { inspectEmailConfig } from './config'; import type { EmailProviderName } from './types';  export type EmailProviderDiagnostics = {   selectedProvider: EmailProviderName;   hasEmailFrom: boolean;   hasReplyTo: boolean;   hasSendGridKey: boolean;   hasResendKey: boolean;   fromSource: 'EMAIL_FROM' | 'SENDGRID_FROM_EMAIL' | 'missing';   dryRun: boolean;   ready: boolean;   issues: string[]; };  export function getEmailProviderDiagnostics(): EmailProviderDiagnostics {   const inspected = inspectEmailConfig();   return {     selectedProvider: inspected.selectedProvider,     hasEmailFrom: inspected.hasEmailFrom,     hasReplyTo: inspected.hasReplyTo,     hasSendGridKey: inspected.hasSendGridKey,     hasResendKey: inspected.hasResendKey,     fromSource: inspected.fromSource,     dryRun: inspected.dryRun,     ready: inspected.ready,     issues: inspected.issues,   }; }