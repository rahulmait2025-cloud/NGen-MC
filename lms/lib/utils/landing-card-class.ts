import { cn } from '@/lib/utils';

/** Shared dark premium card surface for Student LMS landing pages. */
export const studentLandingCardClass = cn(
  'rounded-[2rem] landing-card-premium landing-card-hover py-0 gap-0',
);
